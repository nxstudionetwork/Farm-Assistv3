Fix and complete the existing **Login and Signup/Account Creation pages** of Farm Assist without rebuilding the application or changing unrelated pages.

First fix the major issue: the **“Create Account” option is appearing twice/duplicated on the Login page**. Identify why it is being rendered twice and remove the duplicate at the source. There must be only one correct Create Account option.

Audit the complete Login page for duplicate components, overlapping elements, incorrect navigation, broken buttons, incorrect spacing, console errors, unnecessary UI, and responsive problems.

Keep the existing Farm Assist login flow and authentication architecture. Do not replace the current authentication system unnecessarily.

The Login page should correctly support the existing login methods: **Phone, Email, and Farm ID**, according to the current application flow.

Make sure the Farm ID input correctly handles the existing 8-digit normalization requirement, including values such as `34` becoming `00000034` where appropriate.

Keep the existing **PIN/OTP terminology and flow**. Do not introduce unnecessary “password” wording if the current Farm Assist system uses PIN.

Make sure Login correctly handles invalid input, missing fields, invalid credentials, expired OTP, incorrect OTP, network/API errors, loading states, and successful authentication with clear user-friendly messages.

Make sure the **Create Account** button/link navigates to exactly one Signup flow and never opens duplicate forms or duplicate dialogs.

Audit the complete Signup page and fix all existing UI and functional errors without changing the intended Farm Assist signup flow.

Preserve the existing signup steps such as location/state, district, village, farmer details, phone/email verification, required validation, profile information, and farmer ID generation wherever already implemented.

Make sure every signup field has proper validation, including required fields, phone number format, email format where applicable, and existing identification-number validation rules.

Prevent users from accidentally submitting the signup form multiple times. Show a proper loading state while signup/OTP/API requests are processing.

Make sure OTP generation, OTP verification, resend OTP, expiry handling, and error handling work correctly with the existing backend.

Make sure successful signup creates the correct farmer/account record in the database and does not create duplicate farmer accounts when the same request is submitted twice.

Verify that the generated Farmer ID and related location information are saved correctly and displayed correctly after successful registration.

Make sure Login and Signup use the **real backend/database/authentication flow**, not mock users, fake success responses, hardcoded accounts, or temporary frontend-only authentication.

Check all frontend API calls and backend endpoints used by Login and Signup. Fix incorrect URLs, request formats, response mappings, CORS issues, missing error handling, and inconsistent API configuration using the existing centralized API base URL.

Ensure authentication state is correctly stored/restored after login and that the user is redirected to the correct authenticated starting page.

Make sure logout/session expiration does not leave the user stuck on protected pages.

Fix responsive issues on both Login and Signup for desktop, tablet, and mobile. Make sure forms, dialogs, buttons, images, OTP fields, and navigation never overlap or become clipped.

Keep the existing Farm Assist visual identity, regular font, agricultural styling, logo, and overall design. Improve spacing, alignment, hierarchy, and usability where necessary, but do not redesign the entire application.

Check for duplicated React components, repeated imports, duplicate event handlers, multiple renders, incorrect conditional rendering, stale state, and unnecessary API calls that may be causing the duplicated Create Account option or other UI bugs.

Remove obsolete mock authentication code and unused Login/Signup components only when they are confirmed to be duplicates or no longer used. Do not delete working functionality blindly.

After making the changes, test the complete flows:

Login → choose Phone/Email/Farm ID → enter credentials/OTP/PIN → authenticate → redirect correctly.

Login → Create Account → Signup → validation → OTP verification → account creation → Farmer ID generation → successful login/session.

Also test invalid credentials, invalid OTP, expired OTP, resend OTP, network failure, duplicate submission, refresh during authentication, logout, and returning to Login.

Finally, check the browser console and network requests for errors and fix all Login/Signup-related errors that you find.

Most importantly: **do not rebuild or replace the existing authentication system just to fix UI problems. Inspect the current implementation, fix the root causes, preserve the existing flow, and leave unrelated Farm Assist pages untouched.**
